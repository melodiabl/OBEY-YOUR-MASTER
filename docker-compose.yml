services:
  bot:
    build: .
    container_name: obey_bot
    restart: unless-stopped
    env_file: .env
    network_mode: "host"
    mem_limit: 1200m
    memswap_limit: 1400m
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "5"
